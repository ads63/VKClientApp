# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VKClientApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VKClientApp
  pod 'Alamofire', '~> 5.4'
  
  pod 'RealmSwift'
  pod 'SwiftyJSON', '~> 5.0.0'
  pod "PromiseKit", "~> 6.8"
end
